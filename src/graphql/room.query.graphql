query getRoomDetails($id: ID!) {
  getRoomDetails(id: $id) {
    color
    iconName
    iconType
    id
    name
  }
}

mutation createRoom(
  $groupId: ID!
  $color: String!
  $iconName: String!
  $iconType: IconType!
  $name: String!
) {
  createRoom(
    color: $color
    iconName: $iconName
    iconType: $iconType
    name: $name
    groupId: $groupId
  ) {
    color
    iconName
    iconType
    id
    name
  }
}

mutation updateRoom(
  $color: String!
  $iconName: String!
  $iconType: IconType!
  $name: String!
  $id: ID!
) {
  updateRoom(
    color: $color
    iconName: $iconName
    iconType: $iconType
    name: $name
    id: $id
  ) {
    color
    iconName
    iconType
    id
    name
    tasks {
      id
    }
  }
}

mutation deleteRoom($id: ID!) {
  deleteRoom(id: $id) {
    id
  }
}
