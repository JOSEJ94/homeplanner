query getInvitations {
  getInvitations {
    ...groupMember
  }
}

mutation updateInviteStatus(
  $groupId: ID!
  $newStatus: GroupStatus!
  $userId: ID!
) {
  updateInviteStatus(
    groupId: $groupId
    newStatus: $newStatus
    userId: $userId
  ) {
    ...groupMember
  }
}
